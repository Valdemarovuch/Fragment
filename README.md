# Fragment

<div align="center">

![Python](https://img.shields.io/badge/Python-3.8+-blue.svg)
![Platform](https://img.shields.io/badge/Platform-Windows%20%7C%20macOS%20%7C%20Linux-lightgrey.svg)
![License](https://img.shields.io/badge/License-MIT-green.svg)

**Професійна утиліта для контрольованого пошкодження файлів з графічним інтерфейсом**

[Особливості](#особливості) • [Встановлення](#встановлення) • [Використання](#використання) • [Режими](#режими-пошкодження)

</div>

---

## 📋 Опис

Fragment — це потужна утиліта з графічним інтерфейсом для **контрольованого пошкодження файлів** та **безпечного видалення** конфіденційних даних. Дозволяє застосовувати різні методи corruption або многопрохідного перезапису до файлів будь-яких типів.

### ✨ Особливості

#### 🔧 Основні функції
- 🎨 **Сучасний GUI** на CustomTkinter
- 🔒 **Безпечна робота** - створює копії, оригінали залишаються недоторканими
- 📊 **Детальна статистика** - час обробки, кількість змінених байтів, розміри файлів
- 🔄 **Багатопотоковість** - інтерфейс не зависає під час обробки
- 📁 **Batch обробка** - додавання файлів або цілих папок
- 📈 **Детальне логування** - всі операції записуються у файл

#### 💥 Режим 1: Corruption (Пошкодження)
- 🎯 **5 режимів corruption** - від повного знищення до точкового втручання
- 📝 **Гнучкі шаблони назв** - 6 варіантів іменування вихідних файлів
- 🕐 **ПОВНЕ копіювання метаданих** - ідентичні властивості байт до байта
  - ✅ Системні метадані (дати створення, модифікації, доступу)
  - ✅ Office документи (автор, компанія, коментарі, версія, тощо)
  - ✅ Word (.docx) - всі core properties
  - ✅ Excel (.xlsx) - всі властивості документа
  - ✅ PowerPoint (.pptx) - повні метадані презентації
  - ✅ Зображення - EXIF дані
  - ✅ Windows - час створення файлу (creation time)

#### 🔐 Режим 2: Secure Delete (Безпечне видалення)
- 🛡️ **DoD 5220.22-M стандарт** - перевірений військовий стандарт знищення даних
- 📊 **3 рівні безпеки**:
  - ⚡ **Quick (1 прохід)** - швидке видалення з одним перезаписом
  - ✅ **Standard (3 проходи)** - DoD стандарт (нулі → одиниці → випадкові)
  - 🔐 **Extended (7+ проходів)** - для критичних даних (можна користувацький)
- 🎯 **Затирання метаданих MFT** - перейменування файлу перед видаленням
- 📈 **Статистика видалення** - розмір, час, успіх операції


## 🚀 Встановлення

### Вимоги

- Python 3.8 або новіший
- Модуль `customtkinter`
- Модуль `python-docx` (для метаданих Word)
- Модуль `openpyxl` (для метаданих Excel)
- Модуль `python-pptx` (для метаданих PowerPoint)
- Модуль `Pillow` (для метаданих зображень)
- Модуль `pywin32` (для Windows - час створення файлу)

### Крок 1: Клонування репозиторію

```bash
git clone https://github.com/Valdemarovuch/Fragment.git
cd Fragment
```

### Крок 2: Встановлення залежностей

```bash
pip install -r requirements.txt
```

### Крок 3: Запуск

```bash
python main.py
```

## 💻 Використання

### Базовий workflow

#### 🔴 Режим Corruption (пошкодження):
1. **Додайте файли**
   - Кнопка "+ Add Files" для вибору окремих файлів
   - Кнопка "+ Add Folder" для додавання всієї папки

2. **Переконайтеся, що режим "Corrupt Files"** у dropdown'і Operation

3. **Налаштуйте параметри corruption**
   - Виберіть режим corruption
   - Оберіть шаблон для назв файлів
   - За бажанням вкажіть output директорію

4. **Запустіть обробку**
   - Натисніть "Corrupt All →"
   - Спостерігайте за прогресом у реальному часі

#### 🔵 Режим Secure Delete (видалення):
1. **Додайте файли** (як вище)

2. **Виберіть режим "Secure Delete"** у dropdown'і Operation

3. **Налаштуйте безпеку**
   - Виберіть режим видалення (Quick/Standard/Extended)
   - Або встановіть кількість проходів вручну (1-35)

4. **Запустіть видалення**
   - Натисніть "Secure Delete →"
   - Файли будуть безпечно знищені (перезаписані та видалені)

4. **Перегляньте результати**
   - Детальна статистика після завершення
   - Логи у файлі `fragment_corruptor.log`

### Керування файлами

- **Видалення** - кнопка "×" біля кожного файлу
- **Очищення списку** - кнопка "🗑 Clear All"
- **Статус обробки** - відображається для кожного файлу з часом

## 🔧 Режим 1: Режими пошкодження (Corruption)

### 1. Full Destruction (Повне знищення)
Комбінує всі три методи для максимального ефекту:
- Перезапис заголовка (16 байт)
- Інверсія 5 випадкових байтів
- Обрізання 10-50 байт з кінця

### 2. Header Overwrite Only
Заміна перших 16 байт випадковими даними. Ламає розпізнавання типу файлу операційною системою.

### 3. Bit Flipping Only
Інверсія випадкових байтів всередині файлу. Псує вміст без зміни структури.

### 4. Truncate Only
Видалення випадкової кількості байтів з кінця файлу. Ламає цілісність структур типу ZIP.

### 5. Custom
Власні параметри для кожного методу (байти header, кількість noise, truncate).

## 🔐 Режим 2: Безпечне видалення (Secure Delete)

### Алгоритм DoD 5220.22-M

Fragment реалізує стандарт Міністерства оборони США для безпечного видалення даних:

#### Три рівні безпеки:

| Рівень | Проходи | Патерни | Для чого |
|--------|---------|---------|----------|
| **Quick** | 1 | Випадкові дані | Швидке очищення несерйозних даних |
| **Standard (DoD)** | 3 | Нулі → Одиниці → Випадок | Офіційний DoD стандарт, достатньо для більшості |
| **Extended (Gutmann)** | 7+ | Див. нижче | Критичні дані, максимальна безпека |

#### Процес видалення:
1. **Багатопрохідний перезапис** - філ файл перезаписується n разів:
   - Прохід 1: Всі нулі (`0x00`)
   - Прохід 2: Всі одиниці (`0xFF`)
   - Прохід 3+: Випадкові дані (критично для восстановлення)

2. **Затирання метаданих MFT**:
   - Файл перейменовується 3 рази з випадковими іменами
   - Це затирає сліди в таблиці файлової системи Windows

3. **Видалення**:
   - Остаточне видалення файлу з диску
   - Дані більше не відновні жодними утилітами

### Чому Secure Delete краще за звичайне видалення?

```
Звичайне видалення (Delete):
- Операційна система просто помічає область як "вільну"
- Дані залишаються на диску
- Відновлювачі можуть повністю відновити файл

Secure Delete:
- Дані фізично перезаписуються 3-7 разів
- Метадані затираються на рівні файлової системи
- Навіть спеціалізовані утилити не можуть відновити
```

### Сценарії використання:

- ✅ **Видалення конфіденційних документів** (NDA, контракти)
- ✅ **Очищення перед передачею комп'ютера** (подарунок, продаж)
- ✅ **Видалення персональних даних** (фото, листування)
- ✅ **Архівування с видаленням оригіналу** (шифрований backup)

## 📝 Шаблони назв файлів (Corruption режим)

| Шаблон | Приклад результату |
|--------|-------------------|
| **Same as original** | `document.pdf` |
| **Numbered** | `document (1).pdf` |
| **Prefix** | `[BROKEN] document.pdf` |
| **Suffix** | `document [BROKEN].pdf` |
| **Timestamp** | `document_20260103_153045.pdf` |
| **Custom** | `PREFIX_document_SUFFIX.pdf` |

## 📂 Структура проекту

```
Fragment/
├── main.py                 # Точка входу
├── config.py              # Налаштування та константи
├── gui.py                 # Графічний інтерфейс
├── corruption_engine.py   # Логіка пошкодження файлів
├── requirements.txt       # Залежності
├── README.md             # Документація
└── fragment_corruptor.log # Файл логів (створюється автоматично)
```

## 📊 Статистика

Після обробки програма показує:
- ✅ Кількість успішно оброблених файлів
- ❌ Кількість помилок
- 📦 Загальна кількість змінених байтів
- 💾 Розмір до/після corruption
- ⏱️ Час обробки (загальний та середній)

## ⚙️ Технічні деталі

### Методи corruption

**Header Overwrite:**
```python
f.seek(0)
f.write(os.urandom(16))
```

**Bit Flipping:**
```python
byte = f.read(1)
inverted = int.from_bytes(byte, "big") ^ 0xFF
f.write(inverted.to_bytes(1, "big"))
```

**Truncate:**
```python
f.truncate(file_size - cut_bytes)
```

### Збереження метаданих

Програма відновлює ВСІ метадані оригіналу:
- Дата створення (birthtime на macOS)
- Дата модифікації (mtime)
- Дата доступу (atime)
- Права доступу (permissions)

## 🔐 Безпека

- ✅ Оригінали **ніколи** не змінюються
- ✅ Всі операції виконуються на копіях
- ✅ Валідація доступу до файлів перед обробкою
- ✅ Детальне логування всіх операцій
- ⚠️ Використовуйте тільки на тестових/копіях файлів

## 📝 Приклади використання

### Тестування обробки помилок
Перевірте як ваша програма обробляє пошкоджені документи:
```
1. Додайте тестові файли
2. Режим: Header Overwrite Only
3. Output: окрема тестова папка
4. Запустіть
```

### Batch corruption для QA
Зіпсуйте колекцію файлів для тестування:
```
1. Add Folder з тестовими даними
2. Режим: Full Destruction
3. Naming: Prefix [BROKEN]
4. Запустіть та отримайте детальну статистику
```

## 📄 Ліцензія

MIT License - дивіться файл [LICENSE](LICENSE)

## ⚠️ Disclaimer

Ця утиліта призначена **виключно для легітимного тестування** програмного забезпечення. Автор не несе відповідальності за неправомірне використання.
